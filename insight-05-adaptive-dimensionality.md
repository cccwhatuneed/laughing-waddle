# 维度优化即架构优化（Dimensionality Optimization as Architectural Principle）

> 改变信息收集的维度策略，比增加算力更能提升整体系统性能。

---

## 0. 一句话主张

**固定维度、固定策略的持续采集 < 旋转维度、自适应策略的间歇采集。**

---

## 1. 背景与缘起

在量化交易、实验科学、产品开发等领域，采集策略决定了最终决策的质量。

主流做法是：**持续采集相同的维度**（每天记录相同的指标，每个实验测量相同的变量）。

本洞察提出：**这样做效率太低。** 你应该旋转维度——每一轮采集不同的维度子集，在总预算不变的前提下获得更高的信息密度。

---

## 2. 核心思想

### 2.1 信息效率的数学直观

假设你有 N 个 token（或时间、或样本量）可用。

**策略 A：固定维度**
```
第1轮: 维度 A（采集 N 个数据点）
第2轮: 维度 A（采集 N 个数据点）
第3轮: 维度 A（采集 N 个数据点）
...
第N轮: 维度 A（采集 N 个数据点）

总采集量: N × N = N² 个数据点
但全部是同一维度 → 有效信息 ≈ N（因为重复度高）
```

**策略 B：旋转维度**
```
第1轮: 维度 A（采集 N/√N 个数据点）
第2轮: 维度 B+C（采集 N/√N 个数据点）
第3轮: 维度 D（采集 N/√N 个数据点）
第4轮: 维度 A+B（采集 N/√N 个数据点）
...
第√N轮: 维度 E+F（采集 N/√N 个数据点）

总采集量: √N × (N/√N) × √N = N² 个数据点
但分布在 √N 个不同维度 → 有效信息 ≈ √N × √N = N（正交）
```

关键：**总采集量相同（都是 N²），但策略 B 的正交信息量远高于策略 A。**

### 2.2 为什么旋转维度更有效

1. **避免过拟合**：固定维度系统会过度优化单一视角，忽略其他可能的解释；
2. **早期异常检测**：旋转维度让你在更早的阶段发现"意料之外的模式"；
3. **鲁棒性**：当某一维度失效时（数据源中断、市场 regime 变化），旋转系统已经有其他维度的备份数据；
4. **泛化能力**：跨维度学习比单维度深度挖掘更容易产生可迁移的模式识别能力。

---

## 3. 量化交易中的应用

### 3.1 传统做法

量化交易员通常持续追踪一组固定的因子（动量、价值、波动率等），每天更新相同维度的数据。

**问题：** 因子之间高度共线性，信息冗余严重。

### 3.2 维度旋转策略

将采集预算划分为多个维度窗口：

| 时间窗口 | 采集维度 | 目标 |
|---------|---------|------|
| 周一 | 流动性维度 | 买卖价差、成交量分布 |
| 周二 | 波动率维度 | 隐式波动率曲面、偏度 |
| 周三 | 相关性维度 | 行业间相关性矩阵 |
| 周四 | 情绪维度 | 新闻情感、社交媒体信号 |
| 周五 | 宏观维度 | 利率预期、通胀预期 |

每周一循环，但每 4-5 周旋转一次维度的权重和粒度。

**效果：** 相比固定因子方法，维度旋转策略在同等数据预算下获得更高的信息增益。

---

## 4. 通用性原理

这个原理不仅适用于 AI 上下文 profiling，还适用于：

### 4.1 实验设计

- 固定策略：每次实验改变所有变量 → 低信息效率；
- 旋转策略：每次实验改变一个变量 → 高信息效率（等价于控制变量法）。

### 4.2 投资组合管理

- 固定策略：等权配置 → 所有资产暴露于同一 regime；
- 旋转策略：按 regime 轮换权重 → 不同 regime 下有不同最优配置。

### 4.3 产品调研

- 固定策略：每次访谈问相同问题 → 得到相同类型的答案；
- 旋转策略：第一次问用户行为，第二次问动机，第三次问痛点 → 获得正交的洞察。

---

## 5. 与 SGD 的理论联系

旋转维度策略等价于机器学习中的 **mini-batch stochastic gradient descent**：

| SGD 概念 | 维度旋转 | 解释 |
|----------|---------|------|
| 全批梯度下降 | 固定维度持续采集 | 每一步信息量大但冗余高 |
| mini-batch SGD | 旋转维度间歇采集 | 每一步信息量小但正交 |
| Learning rate | 维度切换频率 | 太快 = 不稳定，太慢 = 欠采样 |
| Momentum | 维度记忆衰减 | 旧维度数据的影响力逐渐减弱 |

小 AI 的维度旋转 = 上下文 profiling 的 SGD。

---

## 6. 实现要点

1. **维度池**：预先定义一组正交的维度；
2. **调度器**：决定每一轮采集哪些维度（可以是随机的，也可以是根据不确定性加权的）；
3. **衰减函数**：旧维度数据的权重随时间衰减；
4. **置信度聚合**：不同维度的数据在聚合时考虑各自的置信度和新鲜度。

---

## 7. 为什么"没人做"

机制都已存在：

- 维度旋转 → 实验设计中的"控制变量法"；
- 调度器 → 数据库中的 ETL 调度；
- 衰减函数 → 时间序列分析中的滑动窗口。

**空位在：把这些机制系统化为一个可迁移的"维度优化即架构优化"原则，并明确其与 SGD 的理论联系。**

目前它是分散在各领域里的隐性知识，没有被统一表达为一个**可应用于 AI 系统设计的架构原则**。

---

## 8. 结语

**架构的本质不是"做什么"，而是"在哪些维度上做"。**

改变维度的策略，比增加算力的投入，更能从根本上改变系统的信息效率。

这不仅仅是一个工程技巧——它是一个**架构优化原则**。

---

*作者：视频编码老手艺人 / 独立系统架构思考者。*
